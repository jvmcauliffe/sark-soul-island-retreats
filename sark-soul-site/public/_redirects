# Cloudflare Pages redirect map for the October 2026 cutover.
# Reconciled against the live Wix sitemap on 5 July 2026 (12 URLs, see
# docs/live-copy). Every live slug exists 1:1 on this site except /blog,
# which the rebuild replaces with /journal. Re-export the sitemap at
# launch freeze and confirm nothing new appeared before DNS cutover.
# Format: /old-path /new-path 301

/blog /journal 301
/blog/* /journal 301

# Retired pages that may still hold external links or index entries.
/spring /retreats-on-sark 301
