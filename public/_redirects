# Chat runs as a Netlify Function at /api/chat, routed by the function's own
# config.path, so it is same-origin with the static site and deploys with it.
# No proxy line is needed here for it.

# Redirect map for the Wix cutover. Slugs verified against the live sitemap:
# these match 1:1 and need no redirect:
#   /  /retreats-on-sark  /why-sark  /the-practice  /our-story
#   /meet-the-team  /sark-island-yoga-retreat-faq
#   /visiting-sark-for-a-wellness-retreat  /journal  /contact
#   /terms-conditions  and the nine room booking pages
#
# Legacy Wix URLs with crawl history. Wix's own redirects die at cutover,
# so they are recreated here.
/spring  /retreats-on-sark  301
/autumn  /retreats-on-sark  301
# The exact links in Monica's newsletters, still opened and clicked. They
# 404'd on the new site, so warm clicks were landing on nothing. Point them
# at the booking page.
/spring-immersion  /retreats-on-sark  301
/autumn-immersion  /retreats-on-sark  301
/immersion-overview  /retreats-on-sark  301
/reserve-place  /retreats-on-sark  301
/blog  /journal  301
/blog/*  /journal/:splat  301
/copy-of-12-sept-sea-lavender-single-1  /12-sept-sea-lavender-shared  301

# Convenience alias: Astro emits sitemap-index.xml, but sitemap.xml is the habit.
/sitemap.xml  /sitemap-index.xml  301
